# lib/ai

Integrasi Google Gemini via Vercel AI SDK (`@ai-sdk/google` + `ai`).
Berisi penyusunan prompt batch, `generateText()` dengan response JSON, dan
parsing hasil ke `modulSections`. Diisi pada slice **AI Generation** (issue #8–#12).
